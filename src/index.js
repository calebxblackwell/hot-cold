import React from 'react';
import ReactDOM from 'react-dom';

import './reset.css';
import './index.css';

import Game from './components/game';

ReactDOM.render(
  <Game />,
  document.getElementById('root')
);



//some of the terms for this challenge i didn't know. Like !==0 I needed a refresher on
//so some of that might not be correct, but a lot of syntax was set up already
//and i tried my best to follow it here.
//i went in and redid all of the areas where props are passed, to make them make
//sense with what you taught me.
